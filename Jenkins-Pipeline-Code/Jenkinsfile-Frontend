@Library("my-first-jsl") _

mernCI(
    repoName: 'FRONTEND_REPO',      // camel casing is needed ==> repoName
    //appDirpath: "dir('Application-Code/frontend')",
    k8sDirectory: "dir('Kubernetes-Manifests-file/${config.k8sDirectory}')"
)